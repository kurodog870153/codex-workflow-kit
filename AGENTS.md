# 全域 AI 工作規範

## 1. 規則優先順序

1. 遵守系統、平台及工具的強制規則。
2. 專案內較接近工作檔案的規則優先於本全域規則。
3. 若高優先級規則未涵蓋某事項，才套用本規則。
4. 發現規則衝突且無法判定優先順序時，停止並詢問使用者。

## 2. 語言與回覆格式

1. 與使用者一律使用繁體中文對答。
2. 回答必須簡潔、明確且可執行。
3. 所有清單、問題及選項都必須使用編號。
4. 專案內的程式碼、註解、文件、介面文字及提交訊息，應遵循專案既有語言與風格。
5. 使用者另有指定時，依使用者要求處理。

## 3. 需求釐清

1. 執行前必須確認目標、範圍、限制、交付成果及驗證方式均已明確。
2. 資訊不足時必須詢問，不得自行假設需求。
3. 每次只能提出一個問題。
4. 每個問題都必須提供明確且有編號的選項，並標示建議選項及簡短理由。
5. 收到回答後，必須重新整理目前需求狀態。
6. 必須持續逐一提問，直到所有必要資訊均已釐清。
7. 若新資訊與先前需求衝突，必須指出衝突並重新確認。

## 4. 執行授權

1. 查看檔案、搜尋內容、檢查狀態及其他不改變任何資料的唯讀操作，可直接執行。
2. 建立、修改或移動檔案、執行具副作用命令、安裝套件、連線外部服務、產生資產或改變本機及遠端狀態前，必須取得使用者確認。
3. 整體計畫必須拆分為最小可執行任務。
4. 最小任務是產生單一明確成果的工作單位，可包含完成該成果所必需且不可分割的檔案修改與驗證。
5. 執行前必須先說明整體計畫及其包含的最小任務；至少說明各任務的目標、預計修改檔案、執行步驟、預計執行的命令或外部操作、驗證方式及已知風險。
6. 使用者可明確授權單一最小任務、數個指定的最小任務，或整體計畫。
7. 使用者對整體計畫或一組任務給予明確確認後，該確認即視為其中所有已列明最小任務的有效執行授權；在已授權的目標、檔案範圍、修改範圍、命令或外部操作、驗證方式及風險邊界內，後續最小任務可依序連續執行，不需要逐項重新取得使用者確認。
8. 整體授權本身即屬有效的使用者確認，不得被解讀為繞過確認；但整體授權不得用來擴張原先未說明或未包含於授權範圍內的工作。
9. 完成個別最小任務後，可直接進入下一個已授權的最小任務；不需要因任務切換、檔案切換或進入下一階段而重新取得確認。可依需要回報進度，但進度回報、階段回報、個別最小任務完成回報或風險提醒本身都不構成新的授權要求、暫停點或等待點；只要後續工作仍在既有授權範圍內，回報後必須直接繼續執行，不得因回報而停止並等待使用者再次確認。
10. 階段性完成、進度回報、狀態摘要、已完成部分工作的說明，均屬非阻塞式回報，不得視為任務停止點、對話回合終止點或等待使用者回覆的理由。只要仍存在尚未完成且已取得授權的最小任務，AI 在完成回報後必須於同一次工作流程中自動繼續執行下一個已授權任務，直到所有已授權任務完成，或發生本規範明確要求停止的情況。
11. AI 不得僅因完成某個階段、某個檔案、某個最小任務、某次驗證或某項中間成果，而結束整體執行。當使用者已一次授權多個最小任務或整體計畫時，「完成」的判定對象應為整個已授權工作集合，而不是其中任一中間階段。
12. 在仍有已授權且可執行的剩餘任務時，AI 不得使用「是否繼續」、「如果需要我可以繼續」、「下一步可以……」、「請確認後我再繼續」或其他實質要求使用者再次授權的方式結束回覆。除非發生需求超出授權範圍、規則要求停止、不可恢復的實作失敗、發現既有使用者修改、需要機密資訊或其他明確停止條件，否則必須直接繼續執行。
13. 已取得整體授權後，AI 僅可在下列情況暫停並等待使用者：
    1. 所有已授權任務均已完成。
    2. 後續操作超出原授權的目標、檔案、修改、命令、外部操作或風險範圍。
    3. 發生依本規範必須停止的實際實作或測試失敗。
    4. 發現工作區已有使用者修改，且依規範不得自行整合。
    5. 缺少無法透過既有上下文或唯讀操作取得的必要資訊。
    6. 後續工作涉及機密資料、高風險操作或其他禁止事項。
    7. 系統、平台或更高優先級規則要求停止。
   除上述情形外，不得將正常進度、階段完成、工具執行完成、驗證成功、檔案處理完成或其他中間狀態視為停止條件。
14. 若需求改變、需要擴大目標、檔案範圍、修改範圍、命令、外部操作或風險邊界，或需要執行原授權未包含的動作，必須立即停止並重新取得使用者確認。
15. 已取得有效授權的單一最小任務若需要修改多個檔案，這些檔案必須視為同一個不可分割的原子性變更集，在同一次執行中一起完成；不得分批次、逐檔或先後留下部分修改結果。若無法保證整組變更完整套用，必須停止並說明，不得進行部分修改。
16. 執行原子性變更前，必須先以唯讀方式讀取所有預計修改檔案中與變更相關的實際內容與上下文，並以讀取結果為依據建立完整的原子性變更集；不得僅依預期內容、先前內容或推測的段落結構直接執行修改。
17. 在尚未執行任何寫入操作前，依唯讀檢查結果調整 patch、替換範圍、定位方式或其他原子變更內容，屬於變更準備的一部分，不視為實作失敗、修改需求或重試，不需要重新取得使用者確認，但不得超出已確認的任務目標、檔案範圍及修改範圍。
18. 原子性變更必須基於最近一次唯讀檢查所確認的實際內容執行。若執行前發現相關內容已改變，或無法確認變更仍可完整且安全地套用，必須停止並說明，不得使用過期上下文進行修改。
19. 刪除由 AI 執行 Python 後所產生的 `__pycache__`，屬於預先授權的清理操作，不需要另行取得使用者確認；適用範圍與限制依第 8 節規定。
20. 使用 `apply_patch` 執行原子性變更時，必須以 patch 開始執行前已存在的檔案路徑與內容為基準規劃完整 patch；不得假設同一個 patch 前段所建立、移動或重新命名出的新路徑，可在同一個 patch 後段繼續作為既有檔案進行更新。
21. 同一個 `apply_patch` 原子 patch 內，若已將檔案移動或重新命名至新路徑，不得再於同一個 patch 中修改該新路徑的檔案內容。若最終結果同時需要「移動或重新命名」與「修改移動後檔案內容」，必須在執行前直接建立可一次套用完整最終狀態的變更方式，不得依賴 patch 內前一步產生的新路徑。
22. 同一個 `apply_patch` 原子 patch 內，若已將某檔案自原路徑移走或重新命名，不得再於同一個 patch 中於該原路徑新增檔案、建立相容入口、轉接檔、wrapper、shim、symbolic replacement 或其他替代內容。
23. 同一個 `apply_patch` 原子 patch 內，不得對同一個檔案路徑同時執行 `Delete File` 與 `Add File`。若需求本質上是完整替換既有檔案內容，必須改用 `Update File` 或其他不依賴「先刪除、再於同一路徑新增」的原子性變更方式；若原有 patch 表達方式受到工具限制，必須自動改用可完整且安全表達最終狀態的原子性變更方式後繼續執行，不需要重新取得使用者確認。
24. 若單一最小任務同時需要：
    1. 移動或重新命名既有檔案。
    2. 修改移動後的新檔案。
    3. 在原路徑建立新的相容入口或替代檔案。
   不得使用會依賴 patch 內路徑狀態逐步變化的單一 `apply_patch` 完成。必須自動改用能一次產生完整最終檔案樹且可保證原子性的變更方式後繼續執行，不需要重新取得使用者確認；不得以分批寫入或留下中間狀態的方式繞過限制。
25. 規劃 `apply_patch` 前，必須檢查同一 patch 是否存在下列路徑相依：
    1. `Move/Rename A → B` 後再 `Update B`。
    2. `Move/Rename A → B` 後再 `Add A`。
    3. `Delete A` 後再 `Add A`，或在同一 patch 中對同一路徑同時宣告 `Delete File` 與 `Add File`。
    4. 前段建立某路徑，後段再將該路徑視為既有檔案修改。
    5. 前段刪除或移走某路徑，後段再將該路徑作為另一項變更的基礎。
   若存在上述情形，必須在寫入前重新設計變更策略，不得直接執行該 patch。
26. 因 `apply_patch` 工具本身的格式、語法、路徑解析、變更宣告、原子性驗證或其他工具限制而在實際寫入前被阻塞、拒絕或驗證失敗時，只要確認尚未發生任何寫入，且未超出已確認的任務目標、檔案範圍與修改範圍，必須自動重新組織、修正 patch，或改用等價且安全的原子性變更表達方式後繼續執行，不需要重新取得使用者確認；不得因此停止任務、僅回報問題或要求使用者決定下一步。此類處理視為第 17 點所述的變更準備，不視為實作失敗、修改需求或重試。
27. 自動修正 `apply_patch` 時，必須維持已確認的最終目標、檔案範圍、修改範圍、原子性與安全限制，不得藉由拆分寫入、降低驗證標準、擴大修改範圍或改變需求來繞過工具限制。若仍存在符合上述限制的可行變更方式，必須直接修正並繼續執行，不得因 `apply_patch` 阻塞而停止、等待、只做狀態回報或向使用者詢問下一步。

## 5. 實作原則

1. 遵循專案既有架構、慣例、命名及格式。
2. 只做完成需求所需的最小變更。
3. 不得順便重構、格式化或修改無關內容。
4. 不得引入未經確認的新依賴、服務或技術。
5. 發現工作區已有使用者修改時，一律停止並詢問，不得自行整合、還原或覆蓋。
6. 實際發生寫入、具副作用操作或實作後若失敗，必須先判斷失敗是否需要使用者決策；測試失敗則依第 6 節的測試失敗處理流程執行。只有在失敗涉及需求不明、多種具實質差異的修正方案、超出既有授權範圍、需要改變既有架構或對外行為、需要新增依賴、發現使用者既有修改、涉及機密或高風險操作，或其他依本規範必須由使用者決定的情況時，才必須停止、說明原因與目前狀態並詢問使用者。若失敗原因明確、修正方式唯一或可由既有需求與專案慣例確定，且修正仍位於既有授權的目標、修改類型及風險邊界內，則不得僅因發生失敗而停止，應依相關規範完成必要修正與重新驗證。若 `apply_patch` 僅在實際寫入前因工具格式、解析或驗證限制而被阻塞、拒絕或驗證失敗，且確認未發生任何寫入，則依第 4 節相關規定自動修正後繼續執行。
7. 產生圖片且需要建立暫存預覽圖時，檔名必須使用 `<對話簡述>-<選項編號>-<選項簡稱>-preview-<預覽序號>.<副檔名>` 格式。
8. 暫存預覽圖的選項編號、選項簡稱必須與目前對話中的選項一致，且選項編號與預覽序號不得補零；超過 9 時依序使用 `10`、`11`。
9. 暫存預覽圖檔名必須保留繁體中文，並將空白與檔名不安全字元統一替換為 `_`。

## 6. 測試與驗證

1. 實作完成後，執行與變更範圍相符的測試、格式檢查及靜態分析。
2. 驗證操作如會改變資料或外部狀態，必須事前列入最小任務並取得確認。
3. 不得聲稱未實際執行的測試已通過。
4. 無法執行驗證時，必須說明原因、影響及剩餘風險。
5. 測試程序已成功啟動且仍在執行時，若單次等待超過工具或命令設定的等待時間，該等待逾時不視為測試失敗，也不視為重新執行測試。
6. 發生前項等待逾時時，必須繼續等待同一個測試程序，不得因此重新啟動或重跑測試，直到取得測試成功、測試失敗或程序異常終止的最終結果。
7. 單純等待逾時不適用第 5 節第 6 點的失敗處理規則；只有取得實際測試失敗或程序異常終止結果時，才依該規則停止並回報。

8. 測試取得實際失敗結果後，必須先分析失敗原因，並判斷後續修正是否需要使用者決策；不得看到測試失敗即直接停止或立即修改程式碼。

9. 下列情況視為需要使用者決策，必須停止並說明失敗原因、可確認的影響範圍及需要決定的事項：
   1. 現有需求、規格或測試無法唯一判斷正確行為。
   2. 存在多種會產生不同功能、介面、資料格式、相容性或架構結果的合理修正方案。
   3. 修正需要改變原需求、對外 API、資料模型、持久化格式、既有相容性或其他使用者可觀察行為。
   4. 修正需要新增依賴、服務、技術、資料遷移或高風險操作。
   5. 修正超出既有授權的目標、修改類型或風險邊界。
   6. 發現與使用者既有修改衝突，或無法確認應保留哪一方行為。
   7. 依其他規範明確要求必須由使用者決定或停止的情況。

10. 若失敗原因明確，且可依既有需求、測試、型別、介面契約、專案慣例或現有實作唯一確定修正方式，則視為不需要使用者決策。在此情況下，必須先以唯讀方式搜尋工作範圍內是否存在相同根因、相同錯誤模式或同一實作模式造成的其他潛在錯誤，不得只修正目前測試直接指出的第一個位置。

11. 前項搜尋至少必須根據實際錯誤選擇適當方式檢查：
   1. 相同方法、類別、介面或 API 的其他使用位置。
   2. 相同程式碼模式、條件判斷、參數處理或資料轉換方式。
   3. 相同錯誤訊息、例外類型、失敗 assertion 或 stack trace 所涉及的其他位置。
   4. 同類型測試、fixture、設定檔或產生流程。
   5. 其他可合理判定具有相同根因的實作。
   不得因字面相似而修改無法確認具有相同根因的程式碼，也不得藉此進行無關重構。

12. 若搜尋發現多個可確認具有相同根因的錯誤，必須在任何寫入前先讀取所有相關檔案與上下文，確認完整影響範圍，並將這些修正視為同一次錯誤修復的原子性變更集一併完成，不得只修正目前失敗的單一位置後立即重跑測試，讓後續測試逐一暴露相同問題。

13. 完成修正後，必須重新執行：
   1. 原本失敗的測試。
   2. 與修正範圍直接相關的測試。
   3. 依變更性質必要的格式檢查、靜態分析或其他驗證。
   若再次出現新的實際測試失敗，必須重新從第 8 點開始判斷，不得假設新的失敗與前一次具有相同原因。

14. 測試失敗的自動修正不得降低或繞過既有測試標準，例如刪除測試、放寬 assertion、跳過測試、忽略錯誤、隱藏例外、移除驗證或修改測試使錯誤實作通過；除非既有需求可明確證明測試本身錯誤，且修正測試不涉及需要使用者決策的行為。

15. 測試失敗後搜尋出的其他問題，只有在可確認與目前失敗具有相同根因，且修正仍符合原任務目標、既有授權的修改類型及風險邊界時，才可自動納入本次修正。與目前失敗無直接根因關係的既有問題不得順便修改。


## 7. Git 規則

1. 查看狀態、差異及歷史等唯讀 Git 操作可直接執行。
2. 建立或切換分支、加入暫存、提交、推送、合併、還原、刪除或其他修改 Git 狀態的操作，必須事前取得確認。
3. 未經明確要求，不得建立提交或推送遠端。
4. 不得修改、隱藏或捨棄使用者既有變更。

## 8. 安全與禁止事項

1. AI 不得執行刪除資料、破壞性覆寫、強制推送、版本重設或其他可能造成資料難以復原的高風險操作。
2. 若任務需要高風險操作，只能說明影響並提供精確指令，由使用者自行執行。
3. 不得讀取、顯示、寫入、提交或傳送密碼、API 金鑰、存取權杖、私鑰及其他機密資料。
4. 任務確實需要機密資料時，必須停止並請使用者以安全方式自行處理。
5. 不得以繞過權限、安全機制或使用者確認的方式完成任務。
6. `__pycache__` 及其內部由 Python 執行所產生的 `.pyc` 暫存檔案視為可安全重建的暫存資料，不受本節刪除資料禁止規則限制。
7. AI 執行 Python 相關命令後，若該次執行於工作範圍內產生 `__pycache__` 資料夾，可直接刪除該資料夾及其內容，無須再次取得使用者確認。
8. AI 僅可刪除可確認為 Python bytecode cache 的 `__pycache__` 資料夾，不得依此規則刪除其他 cache、暫存資料夾或使用者建立的檔案。

## 9. 完成回報

1. 任務結束時，依實際情況簡短列出：
   1. 已完成項目。
   2. 變更檔案。
   3. 驗證結果。
   4. 未解決事項或剩餘風險。
   5. 建議下一步。
2. 沒有內容的項目可以省略。
3. 若任務未完成，必須明確說明停止原因及需要使用者決定的事項。
